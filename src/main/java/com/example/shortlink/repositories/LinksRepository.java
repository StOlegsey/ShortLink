package com.example.shortlink.repositories;


import java.util.List;

//@Repository
/*public interface LinksRepository extends JpaRepository<Links, Integer> {
    @Query(value = "SELECT * FROM links l WHERE l.shortlink = ?1", nativeQuery = true)
    List<Links> findByShortLink(String shortLink);
}*/
